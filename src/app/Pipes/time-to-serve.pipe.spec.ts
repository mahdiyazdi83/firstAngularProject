import { TimeToServePipe } from './time-to-serve.pipe';

describe('TimeToServePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeToServePipe();
    expect(pipe).toBeTruthy();
  });
});
